{
  "name": "web-placeholder",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "serve": "^10.0.2"
  },
  "scripts": {
    "build": "mkdir build",
    "test": "echo \"Error: no test specified\" && exit 0",
    "start": "serve ./data"
  }
}
